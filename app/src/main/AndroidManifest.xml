<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Core permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Packages we need to discover (Minecraft Bedrock / Java editions) -->
    <queries>
        <package android:name="com.mojang.minecraftpe" />
        <package android:name="com.mojang.minecraft" />
        <package android:name="net.kdt.pojavlaunch" />
        <package android:name="com.google.android.tts" />
    </queries>

    <application
        android:name=".MineAvaApp"
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MineAva"
        tools:targetApi="35">

        <activity
            android:name=".OnboardingActivity"
            android:exported="false" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".VoiceSettingsActivity"
            android:exported="false" />

        <activity
            android:name=".DeviceHealthActivity"
            android:exported="false" />

        <activity
            android:name=".DiagnosticsActivity"
            android:exported="false" />

        <activity
            android:name=".PackageManagerActivity"
            android:exported="false" />

        <activity
            android:name=".ServerManagerActivity"
            android:exported="false" />

        <activity
            android:name=".ServerPanelActivity"
            android:exported="false" />

        <activity
            android:name=".CloudLogActivity"
            android:exported="false" />

        <activity
            android:name=".ServerDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".ServerWatchdogActivity"
            android:exported="false" />

        <activity
            android:name=".EncyclopediaActivity"
            android:exported="false" />

        <activity
            android:name=".ServerWizardActivity"
            android:exported="false" />

        <activity
            android:name=".VoiceAssistantActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Displays in-game Minecraft assistant overlay panel while the game is running." />
        </service>

        <!-- Needed to hand the downloaded APK to Android's package installer. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
